import React from 'react';

import OverviewFlow from '@/components/OverviewFlow';

const Home: React.FC = () => {
    return (
        <div style={{ width: 700, height: 700, border: '1px solid tomato' }}>
            <OverviewFlow />
        </div>
    );
};

export default Home;
